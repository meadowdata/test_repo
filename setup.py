from setuptools import setup, find_packages

setup(
    name='meadowrun_test_repo',
    version='0.0.1',
    description='',
    packages=['example_package'],
    install_requires=[
        # 'torch',
        'numpy',
        # 'tqdm',
    ],
)